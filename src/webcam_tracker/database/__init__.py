"""Registered-user database.

Local encrypted store for user profiles, face/reid embeddings, consent
status, and metadata. Parameterized queries only, no raw images stored by
default. Implemented in Stage 2.
"""
